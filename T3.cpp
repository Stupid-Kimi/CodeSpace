#include <bits/stdc++.h>
using namespace std;
const int N = 1005;
int n, k;
double a[N];
int main() {
    cin >> n >> k;
    for (int i = 1; i <= n; i++) cin >> a[i];
    while(k--) {
        
    }
}