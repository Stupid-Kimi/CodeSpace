#include <bits/stdc++.h>
using namespace std;
int n, k, z;
vector <int> a, b;
int main() {
    freopen("in.txt" , "w", stdout);
    n = rand() % 10, k = rand() % n;
    cout << n << ' ' << k << '\n';
    system("./T1 < in.txt > out.txt");
    freopen("out.txt" , "r", stdin);
    cin >> z >> z;
    a.resize(n), b.resize(n);
    for (int &i : a) cin >> i;
    cin >> z;1
    for (int &i : b) cin >> i;

}